#ifndef FECHA_H
#define FECHA_H
#include <string>

class fecha
{
    public:
        fecha();
        virtual ~fecha();

    protected:

    private:
        int dia, mes, anio;
};

#endif // FECHA_H
